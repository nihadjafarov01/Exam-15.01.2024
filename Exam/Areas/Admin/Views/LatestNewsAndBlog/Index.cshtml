@using Exam.ViewModels.LatestNewsAndBlogVMs;
@model List<LatestNewsAndBlogListItemVM>

<a asp-action="Create" type="button" class="btn btn-success">Create</a>

<table class="table table-bordered">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">ImageUrl</th>
            <th scope="col">Title</th>
            <th scope="col">Description</th>
            <th scope="col">AuthorId</th>
            <th colspan="2" scope="col">Action</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count == 0)
        {
            <tr>
                <td class="text-center" colspan="7">No data</td>
            </tr>
        }
        else{
            foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.ImageUrl</td>
                    <td>@item.Title</td>
                    <td>@item.Description</td>
                    <td>@item.AuthorId</td>
                    <form method="post">
                        <td>
                            <a asp-action="Update" asp-route-id="@item.Id" type="submit" class="btn btn-secondary">Update</a>
                            <button asp-action="Delete" asp-route-id="@item.Id" type="submit" class="btn btn-danger">Delete</button>
                        </td>
                    </form>
                </tr>
            }
        }
    </tbody>
</table>